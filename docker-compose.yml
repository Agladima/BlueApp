services:
  backend:
    build: ./backend
    command: npm start
    ports:
      - "3001:3001"
    environment:
      PORT: 3001
  frontend:
    build: ./frontend
    command: npm run dev -- --host 0.0.0.0
    ports:
      - "5173:5173"
